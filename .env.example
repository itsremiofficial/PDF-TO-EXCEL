# Free Google Gemini key for the AI table read (aistudio.google.com/apikey).
# Server-only: this is read by /api/gemini and never shipped to the browser.
# Do NOT rename it to NEXT_PUBLIC_* - that would inline it into the bundle.
GEMINI_API_KEY=
